// This is a half adder.
module half_adder( 
input a, b,
output cout, sum );
// A half adder adds two bits and produces a sum and carry-out. 
// The sum is the bitwise-OR of the two inputs, and the carry-out is the AND of the two inputs.
assign cout = a & b;
assign sum = a | b;
endmodule
